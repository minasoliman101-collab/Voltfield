{
  "name": "voltfield-app",
  "version": "1.0.0",
  "description": "VOLTFIELD Supply Co. — Capacitor app wrapper for the static site",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
